<div class="container" style="margin-top: 0px;">
<div class="row">
<div class="col-md-3">

</div>
      <div class="table-responsive col-md-6">
  <table class="table">
    <tr><th colspan="6"><h3 class="text-center">Admins</h3></th></tr>
    <tr>
      <th width="20%">Admin Id</th>
      <th width="50%">Email</th>
    </tr>
    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.id %></td>
        <td><%= admin.email %></td>
        <td><%= link_to 'Edit', edit_admin_path(admin) ,class:"btn btn-info text-white"%> <%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-danger text-white" %></td>
      </tr>
    <% end %>
 </table>
 <%= link_to 'Add Admin', new_admin_path , class:"btn btn-success text-white"%>
  </table>
</div>

<div class="col-md-3">

</div>

</div>

  </div>